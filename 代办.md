• 发现的主要部署风险如下，未修改任何文件。

1. 高危：默认对外链路没有 TLS。 前端仅监听 HTTP 80，FRP 使用纯 TCP 转发；若直接暴露 WEB_PORT 或启用当前 FRP 配置，登录密码和 Bearer JWT 会明文经过网络，可被截获并在 24 小时内重放。见 docker-compose.yml:37、frontend/nginx.conf:2、      
   deploy/frpc.toml:8。                                                                                                                                                                                                                    
   应在公网入口终止 HTTPS，强制 HTTP 跳转 HTTPS，并启用 HSTS；FRP 应使用 HTTPS vhost/TLS 隧道或只连接到本机反向代理。

2. 高危：被审核隐藏的图片仍可通过旧 URL 公开访问。 代码通过 API 响应隐藏 is_visible=False 的图片，但所有上传目录被 StaticFiles 无鉴权挂载，并由 Nginx 直接代理。任何在审核前获取过链接的人，或缓存/爬虫，仍能访问原文件，导致内容审核和撤下
   机制失效。见 backend/app/main.py:359、frontend/nginx.conf:25。                                                                                                                                                                          
   应把待审核/私有文件放到非公开目录，通过鉴权下载接口或短期签名 URL 提供；审核隐藏后删除、迁移或拒绝原对象路径。

3. 高风险：原始图片被直接公开，EXIF 隐私和图片炸弹未处理。 上传仅检查文件头后原样 write_bytes，再由静态服务输出。照片可能含 GPS、设备型号、拍摄时间等 EXIF；也允许高像素压缩炸弹，影响客户端、审核人员和下游处理。见 backend/app/          
   main.py:830、backend/app/main.py:861、backend/app/virtual_life_packs.py:613。                                                                                                                                                           
   应使用 Pillow/libvips 实际解码，限制像素数和帧数，移除 EXIF 后重编码为受控格式。

4. 高风险：上传、注册和分析接口可被用于耗尽磁盘与 SQLite I/O。 Nginx 接受 55 MB 请求，上传在应用层完整读取后才校验；上传接口没有统一限流或全局存储配额。数据库又会在绝大多数业务写入后创建快照，默认保留 100 份。攻击者可批量注册、上传、删
   后重传，或高频写入扩大磁盘和备份 I/O。见 frontend/nginx.conf:7、backend/app/main.py:2203、backend/app/backup.py:120。                                                                                                                   
   应在 Nginx 增加请求速率、并发连接和上传速率限制；按账号/IP 做上传配额；将备份改为定时、加密且受容量监控的任务。

5. 中风险：容器默认以 root 运行，且后端拥有整个持久化目录写权限。 后端镜像未创建非特权用户，./backend/data 整体可写挂载到 /data。一旦依赖、上传处理或应用出现 RCE，攻击面直接延伸到数据库、备份和上传文件。见 backend/Dockerfile:1、docker-
   compose.yml:24。                                                                                                                                                                                                                        
   应创建专用 UID/GID 运行服务，设置 no-new-privileges、cap_drop: [ALL]、只读根文件系统，并仅为需要写入的目录提供最小权限。

6. 中风险：BEHIND_PROXY=true 信任转发 IP 头，但信任边界没有被限制。 限流直接采用 X-Real-IP / X-Forwarded-For；若后端端口被错误暴露、同网络容器被攻陷，或前置代理未正确覆盖请求头，攻击者可以伪造 IP 绕过登录、注册和 AI 调用限流。见       
   docker-compose.yml:18、backend/app/ratelimit.py:60。                                                                                                                                                                                    
   应只信任明确的反向代理网段，并在入口代理覆盖而非追加这些头；后端不得发布宿主机端口。

7. 中风险：生产镜像供应链未固定到摘要。 python:3.12-slim、node:22-alpine、nginx:1.27-alpine 都是可变标签，后续构建可能引入未审计基础镜像内容。后端还切换到第三方 PyPI 镜像。见 backend/Dockerfile:1、backend/Dockerfile:7、frontend/       
   Dockerfile:20。                                                                                                                                                                                                                         
   应固定镜像 digest，使用可信内部镜像源或验证依赖哈希，并在 CI 执行镜像和依赖漏洞扫描。

8. 中风险：JWT 存在 localStorage，但未配置 CSP。 任何未来 XSS 或第三方脚本供应链问题都能直接读取令牌；当前 Nginx 有基础安全头，但没有 Content-Security-Policy。见 frontend/src/api.js:6、frontend/nginx.conf:9。                           
   应先部署严格 CSP，再评估迁移到 HttpOnly; Secure; SameSite Cookie，并配套 CSRF 防护。

另外，绝不能将 docker-compose.dev.yml:6 用于公网：它暴露 Vite 开发服务器、HMR 和后端 --reload，同时挂载源码。

本次是静态审查，未验证实际公网入口、FRPS 配置、防火墙规则、证书和 .env 中的真实密钥。最优先应先补齐 HTTPS，再修复隐藏图片可直接访问的问题。  